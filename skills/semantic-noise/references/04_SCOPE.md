# Audit Scope

Prefer to audit only:

- The diff / touched files for the current task
- Newly introduced modules, files, public APIs, and symbol names

Avoid repo-wide refactors unless explicitly requested.
